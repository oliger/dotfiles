export DOTFILES="$HOME/.dots"
export EDITOR="nvim"
export VISUAL="nvim"
export PAGER="less"

typeset -gU path
path=(
  /usr/local/share/npm/bin
  /usr/local/{bin,sbin}
  /usr/{bin,sbin}
  /{bin,sbin}
  $PATH
)
